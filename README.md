# TUX-le-Manchot
Bash script that generates a documentation page about TUX, the Linux mascot. The project uses Linux tools to collect system information, generate ASCII art, and automatically build documentation in reStructuredText, HTML, and PDF formats.
